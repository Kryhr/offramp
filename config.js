// Frontend runtime config.
// Leave CASHRA_API empty for demo mode (no backend calls, nothing stored).
// Once the backend is live on your VPS, set this to its URL, e.g.:
//   window.CASHRA_API = 'https://api.your-domain.com';
window.CASHRA_API = '';
window.CASHRA_TOKEN = null;
